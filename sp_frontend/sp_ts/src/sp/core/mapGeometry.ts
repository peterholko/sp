/** Logical on-map geometry for the native 144px presentation. */
export const MAP_HEX_SIZE = 144;
export const MAP_HEX_HALF = MAP_HEX_SIZE / 2;
export const MAP_HEX_QUARTER = MAP_HEX_SIZE / 4;
export const MAP_HEX_THREE_QUARTERS = MAP_HEX_SIZE * 0.75;
export const MAP_RESOURCE_ICON_INSET = MAP_HEX_SIZE / 6;

/** Scale for still-unmigrated 72px map textures in the 144px world. */
export const LEGACY_MAP_TEXTURE_SCALE = MAP_HEX_SIZE / 72;
